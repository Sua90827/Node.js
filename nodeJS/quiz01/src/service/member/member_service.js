const member = require("../../database/member/memberDAO");
const db = ()=>{
    return member;
    //const memeber = list.filter( val =>
                //val.id == id && val.name == name)
                //console.log(mem)
}
module.exports = {db}